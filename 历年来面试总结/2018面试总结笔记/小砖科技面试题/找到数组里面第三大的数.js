/**
 * 找到整数数组里面第三大的数
 * 问题描述： 给定一个非空数组，找到里面第三大的数，并且返回。
 *           如果没有，返回最大值。
 *           要求时间复杂度为O(n).
 * 举个例子：
 *    第一种情况： 输入[3，2，1]     输出应该是1
 *    第二种情况： 输入[1,2]         输出应该是2  //第三大的数不存在,所以返回最大值
 *    第三种情况： 输入[2，2，3，1]   输出应该是1  //存在两个值为2的数，都排第二；第三大且唯一的数1返回
 * @param arr
 */



function findThirdNum(arr){

}